import styled from "styled-components";

export const Container = styled.div`
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    background: #DCF2F1;
    padding: 2.5rem;
    box-shadow: 0 -1px 4px rgba(2, 15, 29, 0.242);
`;